const SEND_TOKEN = {
  SUCCESS: "sendTaskSuccess",
  FAILED: "sendTaskFailure",
};

const STATUS = {
  SUCCESS: "SUCCEEDED",
};

const MESSAGE = {
  OPEN_TD_SUCCESS: "Opening of time deposit account successfully.",
  START_EXECUTION: "State machine execution starts",
};

module.exports = { SEND_TOKEN, STATUS, MESSAGE };
